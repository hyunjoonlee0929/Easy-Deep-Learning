# Easy Deep Learning default configuration

default:
  task_type: classification
  model_type: dnn
  target_column: target

dnn:
  hidden_layers: [128, 64, 32]
  dropout: 0.2
  learning_rate: 0.001
  max_epochs: 200
  patience: 20
  batch_size: 32
